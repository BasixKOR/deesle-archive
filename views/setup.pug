extends wiki

block menu
    li.active
        a(href='#') 설치중
block content
    .jumbotron
        .container
            h1 Deesle의 설치를 시작합니다!
            p 아래 정보를 정확히 입력해주세요.
    .container
        form(method='post',enctype='application/json')
            .form-group
                label(for='username') 위키 이름
                input#username.form-control(type='text', placeholder='당신이 열 위키의 이름은 무엇인가요?', name='wikiname')
            .form-group
                label(for='username') 관리자 ID
                input#username.form-control(type='username', placeholder='ID를 입력하세요.', name='username')
            .form-group
                label(for='email') 관리자 이메일 주소
                input#email.form-control(type='email', placeholder='이메일을 입력하세요', name='email')
            .form-group
                label(for='password') 관리자 암호
                input#password.form-control(type='password', placeholder='암호', name='password')
            .form-group
                label(for='url') MongoDB 주소
                input#url.form-control(type='url', placeholder='mongodb://127.0.0.1/deesle 형태로 작성', name='mongoUrl')
            .form-group
                label(for='frontPage') 대문 이름
                input#frontPage.form-control(type='text', placeholder='문서명', name='frontPage')
            button.btn.btn-default(type="submit") 제출
    